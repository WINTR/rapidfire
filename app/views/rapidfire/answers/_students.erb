<%= render partial: "rapidfire/answers/errors", locals: {answer: answer} %>

<%= f.label :answer_students, answer.question.question_text %>
<%= f.collection_select(:answer_text, @students, :id, :email) %>
